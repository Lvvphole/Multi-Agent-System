# Mutable runtime state / run lineage — regenerated by running the harness.
# These are NOT source; commit verification/evaluation reports (Stages 12-13)
# as durable evidence instead of live mutable state.
state_store.json
idempotency.json
gate_state_log.jsonl
judge_session_log.jsonl
human_approval_log.jsonl
artifact_manifest.json

# Python
__pycache__/
*.pyc
.venv/
